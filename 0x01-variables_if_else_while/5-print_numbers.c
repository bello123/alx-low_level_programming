# include <stdio.h>

/**
 * main - Print all single digit
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0, i < 10; i++)
	{
		printf("%d", i);
	}
	purchar('\n');
	return (0);
}
